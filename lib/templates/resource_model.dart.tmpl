import 'package:dartapi_core/dartapi_core.dart';

class {{ResourceName}} implements Serializable {
  final int? id;
  final String name; // TODO: replace with your actual fields

  const {{ResourceName}}({this.id, required this.name});

  factory {{ResourceName}}.fromMap(Map<String, dynamic> map) {
    return {{ResourceName}}(
      id: map['id'] as int?,
      name: map['name'] as String,
    );
  }

  @override
  Map<String, dynamic> toJson() => {
        if (id != null) 'id': id,
        'name': name,
      };
}
